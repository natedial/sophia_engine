services:
  api:
    build: .
    image: scrivener:latest
    command: ["scrivener", "serve", "--host", "0.0.0.0", "--port", "8000"]
    env_file: .env
    ports:
      - "8000:8000"
    restart: unless-stopped

  scheduler:
    build: .
    image: scrivener:latest
    command: ["scrivener", "scheduler"]
    env_file: .env
    restart: unless-stopped

[project]
name = "scrivener"
version = "0.1.0"
description = "Economic and markets data sourcing platform"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "pandas>=2.2.0",
    "sqlalchemy>=2.0.52",
    "psycopg2-binary>=2.9.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.15.0",
    "fredapi>=0.5.0",
    "apscheduler>=3.10.0",
    "python-dotenv>=1.2.3",
    "typer>=0.27.1",
    "rich>=13.0.0",
    "fastapi>=0.141.1",
    "uvicorn>=0.52.4",
    "beautifulsoup4>=4.12.0",
    "pypdf>=6.16.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.16.4",
    "mypy>=2.3.1",
]

[project.scripts]
scrivener = "src.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true
